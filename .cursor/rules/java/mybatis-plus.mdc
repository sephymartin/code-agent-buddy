---
description: MyBatis-Plus 使用规范
globs: ["**/*Mapper.java", "**/*Dao.java", "**/*DO.java", "**/*Entity.java"]
---

## MyBatis-Plus 使用规范

### Entity 实体规范

#### 基类选择

| 基类 | 包含字段 | 适用场景 |
|------|----------|----------|
| `AbstractBaseEntity` | id + createdTime + updatedTime | 系统表、配置表、C端数据 |
| `AbstractAuditableEntity` | 上述 + createdBy + updatedBy | 后台管理、需追溯操作人 |
| `AbstractTenantBaseEntity` | AbstractBaseEntity + tenantId | 多租户 + 时间审计 |
| `AbstractTenantAuditableEntity` | AbstractAuditableEntity + tenantId | 多租户 + 完整审计 |

#### 可选字段（按需添加）
- **逻辑删除**：`@TableLogic(value = "0", delval = "id") protected Long deleted;`
- **乐观锁**：`@Version protected Integer version;`

#### 命名规范
- 实体类统一以 `*DO` 结尾：`SysUserDO`、`InvestProjectDO`

#### 示例
```java
@Data
@TableName("sys_user")
public class SysUserDO extends AbstractAuditableEntity {
    private String username;
}
```

---

### Dao 层规范
- 统一继承 `CustomBaseMapper<T>`（特殊情况可用 `BaseMapper<T>`）
- 优先使用 `*Dao` 命名，也可使用 `*Mapper` 命名

---

### Default 方法规范

使用 interface default 方法封装常用查询逻辑。

#### 方法命名约定

| 前缀 | 含义 | 返回值 | 数据不存在时 |
|------|------|--------|--------------|
| `getBy*` | 按条件查询单个 | 实体或 null | 返回 null |
| `loadBy*` | 按条件加载单个 | 实体 | 抛出异常 |
| `existsBy*` / `is*Exists` | 检查是否存在 | boolean | 返回 false |
| `selectBy*` | 按条件查询列表 | List | 返回空列表 |

#### 代码复用原则

单个实体对象相关操作优先通过 DAO 的 interface default 方法封装，便于多 Service 复用。

**适用场景：** 单个实体查询、存在性检查、简单业务逻辑封装

**示例：**
```java
public interface SysUserDao extends CustomBaseMapper<SysUserDO> {
    default SysUserDO getByUsername(String username) {
        return selectOne(Wrappers.lambdaQuery(SysUserDO.class)
            .eq(SysUserDO::getUsername, username));
    }
}
```

---

### 分页查询规范

#### 分层设计
```
Controller: PagingQuery / PagingResult（与 MP 解耦）
     ↓
Service: PagingQuery.toMpPage() → Page<T> → PagingResult.from()
     ↓
Dao: Page<T>（MP 原生对象）
```

#### Query 类继承
```java
public class UserQueryDTO extends PagingQuery {
    private String name;
}
```

#### Service 层分页
```java
public PagingResult<SysUserVO> querySysUserPage(SysUserQueryDTO queryDTO) {
    Page<SysUserDO> result = sysUserDao.selectUserPage(queryDTO.toMpPage(), queryDTO);
    return PagingResult.from(result, this::convertToVO);
}
```

#### PagingResult 转换方法

| 方法 | 说明 |
|------|------|
| `PagingResult.from(IPage<T>)` | 直接转换 |
| `PagingResult.from(IPage<S>, Function<S, T>)` | 转换并映射（DO → VO） |
| `PagingResult.from(IPage<S>, List<T>)` | 使用已转换的列表 |
| `PagingResult.empty(PagingQuery)` | 创建空结果 |

---

### 批量操作规范

#### CustomBaseMapper 批量方法

| 方法 | 说明 | 使用场景 |
|------|------|----------|
| `insertBatchSomeColumn` | 批量插入，忽略自增主键 | 常规批量插入 |
| `batchInsertAllColumn` | 批量插入，包含主键字段 | 需要指定主键时 |
| `batchUpdateById` | 按主键批量更新 | 批量修改 |
| `deleteBatchIds` | 按主键批量删除 | 批量删除 |

**大数据量分批处理：**
```java
int batchSize = 5000;
for (int i = 0; i < list.size(); i += batchSize) {
    dao.insertBatchSomeColumn(list.subList(i, Math.min(i + batchSize, list.size())));
}
```

---

### 更新操作规范

#### 核心原则
- ❌ 禁止：先查询完整对象，修改后 `updateById`
- ✅ 推荐：创建新对象，只设置 id 和需更新字段

#### 对比示例

```java
// ❌ 错误：先查询再更新
SaobeiPaymentOrderDO order = dao.selectById(id);
order.setBatchId(batchId);
dao.updateById(order);

// ✅ 正确：新建对象，只设置需更新字段
SaobeiPaymentOrderDO update = new SaobeiPaymentOrderDO();
update.setId(id);
update.setBatchId(batchId);
dao.updateById(update);
```

---

### 其他常用方法

| 方法 | 说明 |
|------|------|
| `loadById(id, "错误信息")` | 加载并校验，不存在时抛异常 |
| `selectAll()` / `selectAll(limit)` | 查询全表（可限制数量） |
| `upsert(entity)` | 插入或更新 |

---
